<?php
/**
 * The public-facing functionality of the plugin.
 *
 * @package ApolloWeb\WPWooCommercePrintifySync\Public
 * @since 1_product_sync', $product_sync, 'sync_products');
        $this->loader->add_action('wpwps_scheduled_order_sync', $order_sync, 'sync_orders');
    }

    /**
     * Run the loader to execute all the hooks.
get_status();
			}
		}

		$progress = array(
			'status'      => $action_status,
			'logs'        => $logs,
			'last_update' => '2025-03-01 13:53:40',
		     */
    public function run() {
        $this->loader->run();
    }
}