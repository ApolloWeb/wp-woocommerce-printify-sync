# Ignore WordPress core files (we don't need them in version control)
wp/
wp-content/
!wp-content/plugins/wp-woocommerce-printify-sync/

# Ignore plugin build artifacts or other generated files
*.log
*.bak
*.swp
*.swo
*.tmp

# Ignore vendor directory (composer dependencies, unless required)
vendor/

# Ignore OS-specific files
.DS_Store
Thumbs.db

# Ignore IDE-specific files (VSCode, JetBrains)
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# Ignore local environment settings (e.g., local `.env` files, wp-config backups)
.env
.env.local

# Ignore files related to Docker (build scripts, Docker files)
docker-compose.override.yml
*.bak

# Ignore Composer's lock file (optional depending on your use case)
composer.lock

# Ignore npm-related files (if any, e.g., for asset management)
node_modules/
package-lock.json
npm-debug.log

#Ignore other build-related files
build.sh
docker-bake.hcl